import * as React from 'react';
import styled from 'styled-components';

const Logo = () => <span className="icon-logo-full" />;

const SLogoImage = styled(Logo)`
  font-size: 50px;
`;

export { SLogoImage };
